import {describe,expect,it} from "vitest"; import {createApplication,moveApplicationKeyboard,transitionApplication,type ApplicationStore} from "@/lib/domain/applications/service"; import {dashboardAggregates} from "@/lib/domain/dashboard/service"; import {resetDemoState} from "@/lib/domain/demo/service"; import {assertAdmin,redactOperationalRecord,setAccountDisabled} from "@/lib/domain/admin/service"; import {deleteAccount,exportUserData,runRetention} from "@/lib/domain/privacy/service";
describe("applications status-machine timeline checklist workspace kanban dnd dashboard outcomes learning aggregates demo admin privacy",()=>{it("tracks applications and keyboard movement",()=>{const s:ApplicationStore={applications:[]}; const a=createApplication(s,{ownerId:"u",jobId:"j"}); transitionApplication(s,{ownerId:"u",applicationId:a.id,to:"applied"}); moveApplicationKeyboard(s,{ownerId:"u",applicationId:a.id,to:"interviewing"}); expect(a.timeline).toHaveLength(3); expect(dashboardAggregates(s.applications).counts.interviewing).toBe(1)}); it("isolates demo state",()=>{expect(resetDemoState().writesDatabase).toBe(false)}); it("protects admin and redacts content",()=>{expect(()=>assertAdmin({id:"u",role:"user"})).toThrow(); const user=setAccountDisabled({id:"a",role:"admin"},{id:"u",role:"user"},true); expect(user.disabled).toBe(true); expect(redactOperationalRecord({resumeContent:"x",status:"ok"})).toEqual({status:"ok"})}); it("exports deletes and retains safely",()=>{const d={id:"u",email:"e",profile:{},applications:[1],audit:[{action:"security.login",at:"now"},{action:"profile.edit",at:"now"}]}; expect(exportUserData(d).applications).toHaveLength(1); expect(deleteAccount(d).email).toBeUndefined(); expect(runRetention({inactiveJobs:[{id:"j1",referenced:false},{id:"j2",referenced:true}],quotaBuckets:["q"]}).retainedJobs).toEqual(["j2"])});});
