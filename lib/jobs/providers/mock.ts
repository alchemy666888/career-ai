import type { JobProvider } from "./types";
export const mockJobProvider: JobProvider = { name: "mock", async search(query) { const limit = Math.min(query.limit ?? 3, 10); return Array.from({ length: limit }, (_, index) => ({ provider: "mock", externalId: `mock-${query.query}-${index}`, title: `${query.query} ${index + 1}`, company: "Synthetic Employer", location: query.location ?? "Remote", workStyle: "remote", description: `Synthetic deterministic job description for ${query.query} with responsibilities, qualifications, accessibility, and enough detail for validation.` })); } };
