export type ApplicationStatus="wishlist"|"saved"|"applying"|"applied"|"interviewing"|"offer"|"rejected"|"withdrawn"|"accepted"|"archived"; export type Application={id:string;ownerId:string;jobId:string;status:ApplicationStatus;timeline:{from?:ApplicationStatus;to:ApplicationStatus;reason?:string;at:string}[];checklist:{label:string;completed:boolean}[];notes:string;outcome?:string}; export type ApplicationStore={applications:Application[]};
const allowed:Record<ApplicationStatus,ApplicationStatus[]>={wishlist:["saved","applying","archived"],saved:["applying","archived"],applying:["applied","archived"],applied:["interviewing","rejected","withdrawn","offer"],interviewing:["offer","rejected","withdrawn"],offer:["accepted","withdrawn"],rejected:["archived"],withdrawn:["archived"],accepted:["archived"],archived:[]};
export function createApplication(store:ApplicationStore,input:{ownerId:string;jobId:string}){const app={id:`app-${store.applications.length+1}`,ownerId:input.ownerId,jobId:input.jobId,status:"applying" as const,timeline:[{to:"applying" as const,reason:"Application created",at:new Date(0).toISOString()}],checklist:["Resume approved","Cover letter ready","Submitted externally"].map(label=>({label,completed:false})),notes:""}; store.applications.push(app); return app}
export function transitionApplication(store:ApplicationStore,input:{ownerId:string;applicationId:string;to:ApplicationStatus;reason?:string}){const app=store.applications.find(a=>a.id===input.applicationId&&a.ownerId===input.ownerId); if(!app) throw new Error("Application not found"); if(!allowed[app.status].includes(input.to)&&input.to!==app.status) throw new Error("Invalid transition"); app.timeline.push({from:app.status,to:input.to,reason:input.reason,at:new Date(0).toISOString()}); app.status=input.to; return app}
export function moveApplicationKeyboard(store:ApplicationStore,input:{ownerId:string;applicationId:string;to:ApplicationStatus}){return transitionApplication(store,{...input,reason:"Keyboard Move to control"})}
